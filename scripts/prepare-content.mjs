#!/usr/bin/env node

/**
 * JWT Learn currently stores docs directly in content/docs/.
 * This script is intentionally a no-op, kept so dev/build lifecycle scripts
 * stay compatible with the shared docs template.
 */
console.log('content/docs is ready');
